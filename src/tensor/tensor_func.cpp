#include "tensor.h"
#include "variable/variable_func.h"
#include <cassert>
#include <optional>

namespace tensor {

// Tensor tanh(Tensor &tensor) {
//   auto test = variable::tanh(tensor.var);
//   auto test2 = Tensor(test);
//
//   return Tensor(variable::tanh(tensor.var));
// }
//
// Tensor relu(Tensor &tensor) { return Tensor(variable::relu(tensor.var)); }
//
// Tensor exp(Tensor &tensor) { return Tensor(variable::exp(tensor.var)); }
//
// Tensor log(Tensor &tensor) { return Tensor(variable::log(tensor.var)); }
//
// Tensor sum(Tensor &tensor, std::optional<int> dim, bool keepdim) {
//   return Tensor(variable::sum(tensor.var, dim, keepdim));
// }
//
// Tensor mean(Tensor &tensor) { return Tensor(variable::mean(tensor.var)); }

} // namespace tensor
